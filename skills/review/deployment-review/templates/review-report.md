# Deployment Hygiene Review — {{PROJECT_NAME}}

**Date:** {{REVIEW_DATE}}
**Project:** {{PROJECT_NAME}}
**Path:** {{PROJECT_PATH}}
**Standard:** `standards/deployment.md` v{{STANDARD_VERSION}}
**Skill:** `skills/review/deployment-review`

---

## Automated Signals

> Checks performed by `scripts/maa-deployment-review.sh`.
> All detection is filesystem-only — no containers were built or executed.
> Signals marked `not checked` were skipped because a prerequisite file was absent.

### Deployment Target (§1)

| Check | Result |
|-------|--------|
| Deployment config present | {{CHECK_DEPLOY_CONFIG}} |
| Local dev config present | {{CHECK_LOCAL_DEV_CONFIG}} |

### Container Image Hygiene (§2–§3)

| Check | Result |
|-------|--------|
| `.dockerignore` present | {{CHECK_DOCKERIGNORE}} |
| Multi-stage build | {{CHECK_MULTISTAGE}} |

### Container Security (§3–§4)

| Check | Result |
|-------|--------|
| Non-root user | {{CHECK_NONROOT_USER}} |
| Base image not using `:latest` | {{CHECK_BASE_IMAGE_VERSION}} |
| `.env` excluded from image | {{CHECK_ENV_EXCLUDED}} |

### Process and Network (§5, §9)

| Check | Result |
|-------|--------|
| Process command defined | {{CHECK_PROCESS_COMMAND}} |
| Port declared | {{CHECK_PORT_DECLARED}} |

### Health Check (§6)

| Check | Result |
|-------|--------|
| Health check defined | {{CHECK_HEALTHCHECK}} |

---

## Manual Findings

> The following cannot be determined from the filesystem alone.
> Review each item and record your findings below.
> Reference the section number in `standards/deployment.md` for remediation guidance.

### Runtime Secret Injection (§3, §8)

- [ ] Secrets are injected at runtime — not baked into the image via `ENV KEY=value` instructions
- [ ] `.env` files with real values are not committed (`.gitignore` contains `.env`)
- [ ] Production environment variables are managed by the platform or a secrets manager

**Findings:**
_No findings — or describe what you observed._

---

### Image Size and Build Quality (§2)

- [ ] Production image size is appropriate — dev dependencies and build tools are not present at runtime
- [ ] Multi-stage build (if present) correctly separates builder and runtime stages
- [ ] `.dockerignore` excludes language-specific artefacts (`node_modules/`, `__pycache__/`, `dist/`, `build/`)

**Findings:**
_No findings — or describe what you observed._

---

### Base Image Versioning (§4)

- [ ] Base image specifies at minimum a major.minor version tag (e.g. `python:3.12-slim`, `node:20-alpine`)
- [ ] Base image digest pinning is used or tracked (stronger reproducibility guarantee)
- [ ] Base image is actively maintained — not an EOL or deprecated version

**Findings:**
_No findings — or describe what you observed._

---

### Health Check Quality (§6)

- [ ] Health check endpoint exists in application code (`/health`, `/healthz`, or equivalent)
- [ ] Health check reflects actual application readiness — not a static always-healthy response
- [ ] Health check interval, timeout, and retries are configured appropriately

**Findings:**
_No findings — or describe what you observed._

---

### Process and Shutdown (§5)

- [ ] Process command runs the production server — not a development server (`nodemon`, `flask run --debug`)
- [ ] Application handles `SIGTERM` gracefully — in-flight requests are drained before exit
- [ ] `STOPSIGNAL` is declared if the application requires a non-default signal

**Findings:**
_No findings — or describe what you observed._

---

### Deployment Procedure (§1, §7)

- [ ] Deployment procedure is documented (README, runbook, or ops doc)
- [ ] Rollback procedure exists and is documented
- [ ] Local dev config (`docker-compose.yml`) reflects the production service configuration

**Findings:**
_No findings — or describe what you observed._

---

## Summary

| Area | Status | Notes |
|------|--------|-------|
| Deployment target | | |
| Container image hygiene | | |
| Container security | | |
| Base image versioning | | |
| Process definition | | |
| Health check | | |
| Local dev parity | | |
| Runtime secret injection | | |
| Deployment procedure | | |

**Overall deployment maturity:** _Minimal / Developing / Mature_

---

## Recommended Actions

_List specific, actionable items here. Reference standards section numbers._

1.
2.
3.

---

## Reviewer Notes

_Optional: context, caveats, or follow-up questions._

---

_Generated by MAA · `maa review deployment` · `standards/deployment.md` v{{STANDARD_VERSION}}_
